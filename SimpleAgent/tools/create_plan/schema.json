{
  "type": "function",
  "function": {
    "name": "create_plan",
    "description": "Create a structured execution plan for a goal.",
    "parameters": {
      "type": "object",
      "properties": {
        "goal": {
          "type": "string",
          "description": "Goal or outcome to plan for."
        },
        "days": {
          "type": "integer",
          "description": "Planning horizon in days.",
          "minimum": 1,
          "maximum": 30
        }
      },
      "required": [
        "goal"
      ]
    }
  }
}
